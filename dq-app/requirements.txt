streamlit>=1.40
pandas>=2.0
pyarrow>=15.0          # reads fixtures/out/*.parquet in local mode

# Workspace mode (DQ_APP_DATA_SOURCE=databricks). Uncommented 2026-09-18 alongside
# the workspace block in app.yaml. Comment both out together if the app is ever put
# back to the self-contained fixture demo, so the deploy stops building two client
# libraries it would never import.
databricks-sql-connector>=3.4
databricks-sdk>=0.30
